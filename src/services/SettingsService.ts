import { getSessionUserId } from './SessionService';

// 🖼️ GET SETTINGS
export const getSettings = async () => {
  // Placeholder for SaaS API
  return null;
};

// 💾 SAVE LOGO PATH
export const saveLogoPath = async (path: string) => {
  // Placeholder for SaaS API
  return false;
};